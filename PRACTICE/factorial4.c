#include <stdio.h>
#include <math.h>

int main()
{
    double n, i, fac = 0;
    scanf("%d", &n);
    a = n + 1;
    b = n * a;
    c = b / 2;

    }
    fac = fac + 1;
    printf("factorial is : %lf", fac);

    return 0;
}
